# Guth-Wang-Bateman-Zahl Probe

**Status:** canon research probe  
**Purpose:** connect the external Kakeya theorem stack to the Claude V6 coherence stack without collapsing density into splitting.

## Probe chain

```text
Bateman
-> split(𝒯_Ω)
-> directional tree complexity
-> Wang-Zahl / Guth-Wang-Zahl density and multiplicity vocabulary
-> δ-tube packet
-> sparse filling
-> high multiplicity
-> (companion) Wang–Wu restriction / two-ends Furstenberg  [docs/research/wang-wu-restriction-decoupling.md]
-> Logx(β_scale)*
-> L²_C
-> K_σ^{reg}
-> trace data
-> κ family
```

## Correct separation

The V6.3 falsification event forces the central distinction:

```text
density statistics ≠ tree statistics
```

GWZ density / multiplicity objects:

```text
Δ_max
λ
μ
C_F
C_KT
N_k
```

Bateman tree object:

```text
split(𝒯_Ω)
```

A single symmetric trace kernel averages direction data and sees density / log-measure. It does not see Bateman splitting.

## What the probe supports

The probe supports a corrected V6 geometry layer:

```text
κ = C(split, Δ_max, μ, Logx(β_scale)*)
```

not

```text
κ ≡ split(𝒯_Ω)
```

and not

```text
κ trace moments <-> split(𝒯_Ω)
```

## Operational consequence

The natural trace-moment realization of the Coherence-Splitting Conjecture is falsified:

```text
κ₂, κ_∞, κ_dens are split-blind.
```

The revised research target is a composite coherence functional:

```text
C_Ω^{V6}
= F(
    split(𝒯_Ω),
    Δ_max(Ω),
    μ(Ω),
    N_k(Ω),
    Logx(β_scale)*
  ).
```

## Logx insertion

The user-canonical term remains:

```text
Logx(β_scale)*
```

with

```text
β_scale = ρ / δ.
```

It is logarithmic smoothing inertia across the scale chain. It does not replace the operator. It sits inside the L²_C coherence architecture.

## Probe verdict

The Guth-Wang-Bateman-Zahl lane is strongest as a structural grounding for the coherence functional, not as direct RH evidence.

Canonical statement:

```text
Bateman measures tree branching.
Wang-Zahl measures saturation and anti-clustering.
Guth-Wang-Zahl organizes density, multiplicity, uniformity, and sticky reduction.
Logx(β_scale)* supplies scale-continuity smoothing.
The V6 operator tests which of these objects survive into trace data.
```

Status: **STRUCTURAL ANALOGY + FORMAL falsification of the naive trace-moment bridge**.
