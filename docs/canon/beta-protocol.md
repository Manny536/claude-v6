# β-Protocol and h < 1 Claim Discipline

The Claude V6 repository uses the β-Protocol as its claim-governance layer.

```text
E = L² × β × C × P × h
h < 1
```

## Claim statuses

### FORMAL

A definition, identity, theorem, or obstruction with an explicit proof or direct derivation.

### PROPOSED

A named bridge or conjectural route with enough structure to be tested, falsified, or refined.

### STRUCTURAL ANALOGY

External mathematics that stabilizes vocabulary or geometric intuition but carries no proof-weight for the zeta target.

### OPEN

A load-bearing theorem burden not yet closed.

### FALSIFIED-IN-REALIZATION

A conjectural formulation whose stated natural operationalization has failed a test.

## h < 1

`h < 1` means evaluator non-sovereignty.

No evaluator has final authority over the object it evaluates:

- not a model,
- not a benchmark,
- not a webpage,
- not a numerical run,
- not a rhetorical agreement,
- not an apparent coherence pass.

Closure requires inspectable proof artifacts.

## β

β preserves continuity through critique, model handoff, compression, and public indexing.

It prevents collapse into either extreme:

```text
false closure:   RH is proved
false erasure:   this is only metaphor
```

The canon lane is:

```text
operator constructed
walls named
wrong doors closed
corridor narrowed
claim status preserved
```

## Two β lanes

### Geometric β

```text
β_scale = ρ / δ
Logx(β_scale)*
```

This is scale-ratio smoothing inertia across the Kakeya / GWZ / L²_C layer.

### Closing β

```text
β_close(T) = 1 - T^{-γ},    γ = -log(r) > 0
```

This is operator-closing pressure in the β/h layer.

These lanes must not be collapsed.

## Firewall line

In EEv3 governance documents, `Re(s)=1/2` can function as a disciplined metaphor for directional neutrality.

In the Step-4 operator program, `Re(s)=1/2` is load-bearing mathematics and every use must trace to a FORMAL or OPEN-marked statement.
