# Prime-Carrying L3 · Forced Trace Route

Prime-carrying L3 is the live forced frontier after the V6.5 bounded-lane closures.

## Status

```text
Prime-carrying L3: LIVE · FORCED
RH: OPEN
Coleman Conjecture: OPEN
Theorem lift: OPEN
h < 1
```

## Required architecture

A viable prime-carrying trace route must supply:

```text
lengths: log(p^k)
weights: Λ(p^k)p^{-k/2}
density: Riemann–von Mangoldt T log T
carrier: measure-pair / function-system data
preferred route: Nyman–Beurling / Báez-Duarte
ledger home: Π_1/2
```

## Negative boundary

The square-difference `K_σ` lane cannot supply the required prime content. The bounded coupling and bounded relative-determinant lanes are closed-negative by the Weyl-window law / Theorem H.

## Missing rung

The load-bearing missing rung is an explicit operator construction:

```text
μ(placement) -> ζ-zero location
```

on a prime-carrying ladder.

## KREIN-RANK1 face

KREIN-RANK1 is registered as a wall/face, not as a solved route. Krein spectral shift is measure-pair data. Rank-one / trace-class / KSSF-mediated moves do not inject the prime trace architecture into the n^4 ladder.
